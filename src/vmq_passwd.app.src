{application, vmq_passwd,
 [
  {description, "Simple User Management Plugin for VerneMQ"},
  {vsn, git},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  crypto
                 ]},
  {mod, { vmq_passwd_app, []}},
  {env, [
      {file, "priv/test.passwd"},
      {interval, 5000},
      {vmq_plugin_hooks, [
        {vmq_passwd, auth_on_register, 4}
        ]}
    ]}
 ]}.
