{erl_opts, [debug_info, {parse_transform, lager_transform}]}.
{plugins, [rebar3_diameter_compiler]}.
{provider_hooks, [
    {pre, [
        {compile, {diameter, compile}},
        {clean, {diameter, clean}}
    ]}
]}.
{deps, [
        lager,
        sext,

        {swc, {git, "git://github.com/ricardobcl/ServerWideClocks.git", {branch, "master"}}},
        {sext, "1.4.1"},
        {rocksdb, "0.17.0"},

        %% plumtree related
        riak_dt
       ]}.

{cover_enabled, true}.
